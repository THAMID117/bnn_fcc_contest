# BNN_FCC Contest Report Source

The canonical submission report is authored in [report.tex](report.tex).

Build the PDF from the repository root with either:

```bash
latexmk -pdf report.tex
```

or:

```bash
pdflatex report.tex
pdflatex report.tex
```

The generated file for submission is `report.pdf`.
